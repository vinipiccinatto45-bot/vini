<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NeoCup Vision — A caneca inteligente</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;900&family=Space+Grotesk:wght@300;400;500;700&display=swap" rel="stylesheet" />
  <style>
    /* ============================================================
       RESET & BASE
    ============================================================ */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --black:       #050505;
      --gray-dark:   #111111;
      --gray-mid:    #1a1a1a;
      --gray-line:   #222222;
      --gray-muted:  #555555;
      --white:       #ffffff;
      --white-dim:   rgba(255,255,255,.07);
      --white-border:rgba(255,255,255,.09);
      --neon:        #00c8ff;
      --neon-dim:    rgba(0,200,255,.15);
      --neon-glow:   rgba(0,200,255,.35);
      --metal:       #c0c8d8;
      --font-display:'Space Grotesk', sans-serif;
      --font-body:   'Inter', sans-serif;
    }

    html { scroll-behavior: smooth; font-size: 16px; }

    body {
      background: var(--black);
      color: var(--white);
      font-family: var(--font-body);
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    ::selection { background: var(--neon-dim); color: var(--white); }

    img { display: block; max-width: 100%; }

    a { color: inherit; text-decoration: none; }

    /* ============================================================
       SCROLLBAR
    ============================================================ */
    ::-webkit-scrollbar { width: 4px; }
    ::-webkit-scrollbar-track { background: var(--black); }
    ::-webkit-scrollbar-thumb { background: var(--gray-line); border-radius: 2px; }

    /* ============================================================
       NAVBAR
    ============================================================ */
    .nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      padding: 0 40px;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: background .4s, backdrop-filter .4s, border-color .4s;
      border-bottom: 1px solid transparent;
    }

    .nav.scrolled {
      background: rgba(5,5,5,.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-color: var(--white-border);
    }

    .nav-logo {
      font-family: var(--font-display);
      font-size: 1.15rem;
      font-weight: 700;
      letter-spacing: -.02em;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .nav-logo-dot {
      width: 8px; height: 8px;
      border-radius: 50%;
      background: var(--neon);
      box-shadow: 0 0 8px var(--neon);
      animation: pulse-dot 2.5s ease-in-out infinite;
    }

    @keyframes pulse-dot {
      0%,100% { opacity: 1; transform: scale(1); }
      50% { opacity: .5; transform: scale(.7); }
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 36px;
      list-style: none;
    }

    .nav-links a {
      font-size: .82rem;
      font-weight: 400;
      letter-spacing: .04em;
      color: rgba(255,255,255,.6);
      transition: color .25s;
    }

    .nav-links a:hover { color: var(--white); }

    .nav-cta {
      background: var(--white);
      color: var(--black) !important;
      font-weight: 600 !important;
      padding: 8px 22px;
      border-radius: 100px;
      letter-spacing: .02em !important;
      transition: background .25s, transform .2s !important;
    }

    .nav-cta:hover { background: var(--neon) !important; transform: translateY(-1px); }

    /* hamburger */
    .nav-hamburger {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
      padding: 4px;
    }

    .nav-hamburger span {
      width: 24px; height: 2px;
      background: var(--white);
      border-radius: 2px;
      transition: transform .3s, opacity .3s;
    }

    .nav-hamburger.open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
    .nav-hamburger.open span:nth-child(2) { opacity: 0; }
    .nav-hamburger.open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

    .nav-mobile {
      display: none;
      position: fixed;
      inset: 64px 0 0;
      background: rgba(5,5,5,.97);
      backdrop-filter: blur(24px);
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 40px;
      z-index: 99;
    }

    .nav-mobile.open { display: flex; }

    .nav-mobile a {
      font-size: 1.6rem;
      font-family: var(--font-display);
      font-weight: 300;
      color: rgba(255,255,255,.7);
      transition: color .2s;
    }

    .nav-mobile a:hover { color: var(--white); }

    /* ============================================================
       HERO
    ============================================================ */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 120px 24px 80px;
      position: relative;
      overflow: hidden;
    }

    /* radial ambient glow */
    .hero::before {
      content: '';
      position: absolute;
      top: 10%; left: 50%;
      transform: translateX(-50%);
      width: 700px; height: 700px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(0,200,255,.08) 0%, transparent 70%);
      pointer-events: none;
    }

    .hero-eyebrow {
      font-size: .72rem;
      letter-spacing: .22em;
      text-transform: uppercase;
      color: var(--neon);
      margin-bottom: 24px;
      opacity: 0;
      transform: translateY(12px);
      animation: fade-up .7s .3s forwards;
    }

    .hero-title {
      font-family: var(--font-display);
      font-size: clamp(3rem, 9vw, 7.5rem);
      font-weight: 700;
      line-height: .95;
      letter-spacing: -.04em;
      background: linear-gradient(160deg, #fff 30%, #8ab4f8 70%, var(--neon) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      opacity: 0;
      transform: translateY(18px);
      animation: fade-up .8s .5s forwards;
    }

    .hero-sub {
      margin-top: 24px;
      font-size: clamp(.95rem, 2.2vw, 1.25rem);
      font-weight: 300;
      color: rgba(255,255,255,.55);
      max-width: 520px;
      line-height: 1.65;
      opacity: 0;
      transform: translateY(14px);
      animation: fade-up .8s .75s forwards;
    }

    .hero-actions {
      margin-top: 44px;
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      justify-content: center;
      opacity: 0;
      transform: translateY(14px);
      animation: fade-up .8s .95s forwards;
    }

    .btn-primary {
      background: var(--white);
      color: var(--black);
      font-weight: 600;
      font-size: .9rem;
      letter-spacing: .02em;
      padding: 15px 36px;
      border-radius: 100px;
      border: none;
      cursor: pointer;
      transition: background .25s, transform .2s, box-shadow .25s;
    }

    .btn-primary:hover {
      background: var(--neon);
      transform: translateY(-2px);
      box-shadow: 0 10px 40px var(--neon-glow);
    }

    .btn-outline {
      background: transparent;
      color: var(--white);
      font-weight: 500;
      font-size: .9rem;
      letter-spacing: .02em;
      padding: 14px 36px;
      border-radius: 100px;
      border: 1px solid rgba(255,255,255,.25);
      cursor: pointer;
      transition: border-color .25s, background .25s, transform .2s;
    }

    .btn-outline:hover {
      border-color: var(--white);
      background: rgba(255,255,255,.06);
      transform: translateY(-2px);
    }

    /* cup illustration */
    .hero-cup-wrap {
      margin-top: 72px;
      position: relative;
      opacity: 0;
      transform: translateY(24px) scale(.97);
      animation: fade-up .9s 1.1s forwards;
    }

    .hero-cup {
      width: min(420px, 80vw);
      filter: drop-shadow(0 30px 80px rgba(0,200,255,.25));
      animation: float 4s ease-in-out infinite;
    }

    @keyframes float {
      0%,100% { transform: translateY(0); }
      50% { transform: translateY(-14px); }
    }

    /* scroll hint */
    .hero-scroll {
      position: absolute;
      bottom: 36px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 6px;
      opacity: .35;
      animation: fade-in 1s 2s forwards;
    }

    .hero-scroll span {
      font-size: .65rem;
      letter-spacing: .18em;
      text-transform: uppercase;
    }

    .hero-scroll-line {
      width: 1px; height: 36px;
      background: linear-gradient(to bottom, var(--white), transparent);
      animation: scroll-line 1.8s ease-in-out infinite;
    }

    @keyframes scroll-line {
      0%,100% { transform: scaleY(1); opacity: 1; }
      50% { transform: scaleY(.5); opacity: .3; }
    }

    /* ============================================================
       SECTION SHARED
    ============================================================ */
    section { padding: 120px 40px; }

    .section-label {
      font-size: .68rem;
      letter-spacing: .22em;
      text-transform: uppercase;
      color: var(--neon);
      margin-bottom: 20px;
    }

    .section-title {
      font-family: var(--font-display);
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 700;
      letter-spacing: -.03em;
      line-height: 1.05;
      max-width: 700px;
    }

    .section-desc {
      margin-top: 20px;
      font-size: 1rem;
      color: rgba(255,255,255,.45);
      line-height: 1.75;
      max-width: 520px;
    }

    /* reveal on scroll */
    .reveal {
      opacity: 0;
      transform: translateY(32px);
      transition: opacity .7s ease, transform .7s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* ============================================================
       TECH SECTION
    ============================================================ */
    .tech {
      background: var(--gray-dark);
      position: relative;
      overflow: hidden;
    }

    .tech::before {
      content: '';
      position: absolute;
      right: -200px; top: 50%;
      transform: translateY(-50%);
      width: 500px; height: 500px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(0,200,255,.06) 0%, transparent 70%);
      pointer-events: none;
    }

    .tech-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    .tech-header { margin-bottom: 72px; }

    .tech-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2px;
    }

    .tech-item {
      background: var(--white-dim);
      border: 1px solid var(--white-border);
      padding: 40px 36px;
      transition: background .3s, border-color .3s;
      position: relative;
      overflow: hidden;
    }

    .tech-item::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0;
      width: 0; height: 2px;
      background: var(--neon);
      transition: width .4s;
    }

    .tech-item:hover { background: rgba(255,255,255,.1); border-color: rgba(255,255,255,.15); }
    .tech-item:hover::after { width: 100%; }

    .tech-icon {
      font-size: 1.8rem;
      margin-bottom: 20px;
      display: block;
    }

    .tech-item-title {
      font-family: var(--font-display);
      font-size: 1.05rem;
      font-weight: 600;
      margin-bottom: 10px;
      letter-spacing: -.01em;
    }

    .tech-item-desc {
      font-size: .88rem;
      color: rgba(255,255,255,.45);
      line-height: 1.65;
    }

    /* ============================================================
       HOW IT WORKS
    ============================================================ */
    .how { background: var(--black); }

    .how-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    .how-header { margin-bottom: 80px; text-align: center; }
    .how-header .section-title { margin: 0 auto; }
    .how-header .section-desc { margin: 20px auto 0; }

    .how-steps {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2px;
      position: relative;
    }

    .how-step {
      background: var(--white-dim);
      border: 1px solid var(--white-border);
      padding: 52px 40px;
      text-align: center;
      transition: background .3s;
    }

    .how-step:hover { background: rgba(255,255,255,.08); }

    .how-step-num {
      font-family: var(--font-display);
      font-size: 4rem;
      font-weight: 700;
      letter-spacing: -.05em;
      color: transparent;
      -webkit-text-stroke: 1px rgba(255,255,255,.12);
      line-height: 1;
      margin-bottom: 20px;
    }

    .how-step-icon {
      width: 56px; height: 56px;
      border-radius: 50%;
      background: var(--neon-dim);
      border: 1px solid var(--neon-glow);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      font-size: 1.4rem;
    }

    .how-step-title {
      font-family: var(--font-display);
      font-size: 1.2rem;
      font-weight: 600;
      margin-bottom: 12px;
      letter-spacing: -.02em;
    }

    .how-step-desc {
      font-size: .88rem;
      color: rgba(255,255,255,.45);
      line-height: 1.7;
    }

    /* ============================================================
       GALLERY
    ============================================================ */
    .gallery { background: var(--gray-dark); }

    .gallery-inner {
      max-width: 1200px;
      margin: 0 auto;
    }

    .gallery-header { margin-bottom: 64px; }

    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
    }

    .gallery-card {
      aspect-ratio: 3/4;
      border-radius: 16px;
      position: relative;
      overflow: hidden;
      cursor: pointer;
      border: 1px solid var(--white-border);
      transition: transform .35s, box-shadow .35s;
    }

    .gallery-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 30px 60px rgba(0,0,0,.6), 0 0 0 1px rgba(0,200,255,.2);
    }

    /* cup + LED screen simulation via CSS */
    .gallery-card-inner {
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      background: var(--gray-mid);
    }

    /* the "mug" shape */
    .mock-cup {
      position: relative;
      width: 80px; height: 90px;
    }

    .mock-cup-body {
      width: 80px; height: 90px;
      border-radius: 4px 4px 12px 12px;
      background: #111;
      border: 2px solid #2a2a2a;
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .mock-cup-handle {
      position: absolute;
      right: -20px; top: 20px;
      width: 20px; height: 36px;
      border: 2px solid #2a2a2a;
      border-left: none;
      border-radius: 0 8px 8px 0;
    }

    .mock-screen {
      width: 54px; height: 40px;
      border-radius: 3px;
      overflow: hidden;
      position: relative;
    }

    /* gallery-specific screen contents */
    .screen-1 { background: linear-gradient(135deg, #0d0d2b, #1a0050, #003080, #00c8ff); }
    .screen-2 { background: linear-gradient(160deg, #0a001a, #500000, #ff0060, #ff6a00); }
    .screen-3 { background: #000; display: grid; grid-template-columns: repeat(8,1fr); }
    .screen-4 { background: linear-gradient(180deg, #001a10, #00ff90, #007a4a); }
    .screen-5 { background: linear-gradient(90deg, #1a1a00, #ffcc00, #ff6600); }

    .pixel { width: 100%; aspect-ratio: 1; }

    .gallery-card-label {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      padding: 16px;
      background: linear-gradient(to top, rgba(0,0,0,.85), transparent);
      font-size: .78rem;
      font-weight: 500;
      letter-spacing: .06em;
      text-transform: uppercase;
      color: rgba(255,255,255,.7);
    }

    /* ============================================================
       SPECS
    ============================================================ */
    .specs { background: var(--black); }

    .specs-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    .specs-header { margin-bottom: 72px; }

    .specs-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 24px;
    }

    .spec-card {
      background: rgba(255,255,255,.03);
      border: 1px solid var(--white-border);
      border-radius: 20px;
      padding: 40px 32px;
      transition: background .3s, transform .3s;
    }

    .spec-card:hover {
      background: rgba(255,255,255,.07);
      transform: translateY(-4px);
    }

    .spec-icon {
      font-size: 1.6rem;
      margin-bottom: 20px;
    }

    .spec-name {
      font-family: var(--font-display);
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--white);
    }

    .spec-detail {
      font-size: .85rem;
      color: rgba(255,255,255,.4);
      line-height: 1.6;
    }

    .spec-badge {
      display: inline-block;
      margin-top: 14px;
      font-size: .68rem;
      letter-spacing: .12em;
      text-transform: uppercase;
      color: var(--neon);
      border: 1px solid var(--neon-glow);
      padding: 4px 12px;
      border-radius: 100px;
    }

    /* ============================================================
       TESTIMONIALS
    ============================================================ */
    .testimonials { background: var(--gray-dark); }

    .testimonials-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    .testimonials-header { margin-bottom: 64px; text-align: center; }
    .testimonials-header .section-title { margin: 0 auto; }

    .testimonials-track {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
    }

    .testimonial-card {
      background: rgba(255,255,255,.04);
      border: 1px solid var(--white-border);
      border-radius: 20px;
      padding: 40px 36px;
      position: relative;
      transition: background .3s, transform .3s;
    }

    .testimonial-card:hover {
      background: rgba(255,255,255,.08);
      transform: translateY(-4px);
    }

    .testimonial-stars {
      color: var(--neon);
      font-size: 1rem;
      letter-spacing: 2px;
      margin-bottom: 20px;
    }

    .testimonial-quote {
      font-size: 1.05rem;
      font-style: italic;
      color: rgba(255,255,255,.8);
      line-height: 1.65;
      margin-bottom: 28px;
    }

    .testimonial-author { display: flex; align-items: center; gap: 14px; }

    .testimonial-avatar {
      width: 42px; height: 42px;
      border-radius: 50%;
      border: 1px solid var(--white-border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      background: var(--white-dim);
    }

    .testimonial-name {
      font-size: .88rem;
      font-weight: 600;
    }

    .testimonial-role {
      font-size: .78rem;
      color: rgba(255,255,255,.35);
    }

    /* ============================================================
       CTA
    ============================================================ */
    .cta-section {
      background: var(--black);
      text-align: center;
      padding: 160px 40px;
      position: relative;
      overflow: hidden;
    }

    .cta-section::before {
      content: '';
      position: absolute;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      width: 800px; height: 800px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(0,200,255,.07) 0%, transparent 65%);
      pointer-events: none;
    }

    .cta-label { margin-bottom: 20px; }

    .cta-title {
      font-family: var(--font-display);
      font-size: clamp(2.2rem, 6vw, 4.5rem);
      font-weight: 700;
      letter-spacing: -.04em;
      line-height: 1.05;
      max-width: 680px;
      margin: 0 auto 48px;
    }

    .cta-price {
      display: block;
      font-size: .9rem;
      color: rgba(255,255,255,.35);
      margin-bottom: 32px;
      letter-spacing: .05em;
    }

    /* ============================================================
       FOOTER
    ============================================================ */
    footer {
      background: var(--gray-dark);
      border-top: 1px solid var(--white-border);
      padding: 80px 40px 40px;
    }

    .footer-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    .footer-top {
      display: grid;
      grid-template-columns: 2fr repeat(3,1fr);
      gap: 48px;
      padding-bottom: 64px;
      border-bottom: 1px solid var(--white-border);
    }

    .footer-brand-name {
      font-family: var(--font-display);
      font-size: 1.3rem;
      font-weight: 700;
      letter-spacing: -.02em;
      margin-bottom: 14px;
    }

    .footer-brand-desc {
      font-size: .85rem;
      color: rgba(255,255,255,.35);
      line-height: 1.7;
      max-width: 280px;
    }

    .footer-social {
      margin-top: 28px;
      display: flex;
      gap: 14px;
    }

    .footer-social a {
      width: 36px; height: 36px;
      border-radius: 50%;
      border: 1px solid var(--white-border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: .9rem;
      color: rgba(255,255,255,.4);
      transition: border-color .25s, color .25s;
    }

    .footer-social a:hover { border-color: var(--white); color: var(--white); }

    .footer-col-title {
      font-size: .75rem;
      letter-spacing: .16em;
      text-transform: uppercase;
      color: rgba(255,255,255,.25);
      margin-bottom: 20px;
    }

    .footer-col-links {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .footer-col-links a {
      font-size: .88rem;
      color: rgba(255,255,255,.5);
      transition: color .25s;
    }

    .footer-col-links a:hover { color: var(--white); }

    .footer-bottom {
      padding-top: 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 16px;
    }

    .footer-copy {
      font-size: .78rem;
      color: rgba(255,255,255,.2);
    }

    .footer-legal {
      display: flex;
      gap: 24px;
    }

    .footer-legal a {
      font-size: .78rem;
      color: rgba(255,255,255,.2);
      transition: color .25s;
    }

    .footer-legal a:hover { color: rgba(255,255,255,.5); }

    /* ============================================================
       ANIMATIONS
    ============================================================ */
    @keyframes fade-up {
      to { opacity: 1; transform: translateY(0) scale(1); }
    }

    @keyframes fade-in {
      to { opacity: .35; }
    }

    /* ============================================================
       RESPONSIVE
    ============================================================ */
    @media (max-width: 900px) {
      section { padding: 90px 24px; }
      .nav-links { display: none; }
      .nav-hamburger { display: flex; }
      .nav { padding: 0 24px; }

      .how-steps { grid-template-columns: 1fr; }
      .footer-top { grid-template-columns: 1fr 1fr; }
    }

    @media (max-width: 600px) {
      .hero { padding: 100px 20px 60px; }
      .gallery-grid { grid-template-columns: repeat(2,1fr); }
      .footer-top { grid-template-columns: 1fr; }
      .footer-bottom { flex-direction: column; align-items: flex-start; }
    }

    /* ============================================================
       SVG CUP — inline (drawn via JS)
    ============================================================ */
  </style>
</head>
<body>

  <!-- ===================== NAVBAR ===================== -->
  <nav class="nav" id="nav">
    <a href="#" class="nav-logo">
      <span class="nav-logo-dot"></span>
      NeoCup Vision
    </a>

    <ul class="nav-links">
      <li><a href="#tech">Tecnologia</a></li>
      <li><a href="#how">Como Funciona</a></li>
      <li><a href="#gallery">Galeria</a></li>
      <li><a href="#specs">Especificações</a></li>
      <li><a href="#cta" class="nav-cta">Comprar</a></li>
    </ul>

    <div class="nav-hamburger" id="hamburger" aria-label="Abrir menu">
      <span></span><span></span><span></span>
    </div>
  </nav>

  <!-- mobile menu -->
  <div class="nav-mobile" id="mobileMenu">
    <a href="#tech"   onclick="closeMobile()">Tecnologia</a>
    <a href="#how"    onclick="closeMobile()">Como Funciona</a>
    <a href="#gallery" onclick="closeMobile()">Galeria</a>
    <a href="#specs"  onclick="closeMobile()">Especificações</a>
    <a href="#cta"    onclick="closeMobile()" style="color:var(--neon)">Comprar Agora</a>
  </div>

  <!-- ===================== HERO ===================== -->
  <section class="hero" id="home">
    <p class="hero-eyebrow">Apresentando · 2025</p>
    <h1 class="hero-title">NeoCup<br/>Vision</h1>
    <p class="hero-sub">A caneca inteligente que transforma suas imagens em experiências únicas, na palma da sua mão.</p>

    <div class="hero-actions">
      <button class="btn-primary" onclick="document.getElementById('cta').scrollIntoView({behavior:'smooth'})">Comprar Agora</button>
      <button class="btn-outline" onclick="document.getElementById('tech').scrollIntoView({behavior:'smooth'})">Saiba Mais</button>
    </div>

    <!-- Animated SVG Cup -->
    <div class="hero-cup-wrap">
      <svg class="hero-cup" viewBox="0 0 320 380" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <!-- matte black gradient -->
          <linearGradient id="cupGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%"   stop-color="#1a1a1a"/>
            <stop offset="40%"  stop-color="#0d0d0d"/>
            <stop offset="100%" stop-color="#050505"/>
          </linearGradient>
          <!-- LED screen gradient — animated -->
          <linearGradient id="ledGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%"   stop-color="#001aff">
              <animate attributeName="stop-color" values="#001aff;#00c8ff;#9000ff;#001aff" dur="4s" repeatCount="indefinite"/>
            </stop>
            <stop offset="100%" stop-color="#00c8ff">
              <animate attributeName="stop-color" values="#00c8ff;#9000ff;#001aff;#00c8ff" dur="4s" repeatCount="indefinite"/>
            </stop>
          </linearGradient>
          <!-- glow filter -->
          <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
            <feGaussianBlur stdDeviation="4" result="blur"/>
            <feComposite in="SourceGraphic" in2="blur" operator="over"/>
          </filter>
          <!-- cup shadow -->
          <filter id="shadow" x="-30%" y="-10%" width="160%" height="140%">
            <feDropShadow dx="0" dy="16" stdDeviation="24" flood-color="#00c8ff" flood-opacity="0.2"/>
          </filter>
          <clipPath id="screenClip">
            <rect x="72" y="148" width="148" height="104" rx="6"/>
          </clipPath>
        </defs>

        <!-- cup body -->
        <g filter="url(#shadow)">
          <!-- main body -->
          <path d="M 55 70 L 45 340 Q 45 358 65 358 L 255 358 Q 275 358 275 340 L 265 70 Z"
                fill="url(#cupGrad)" stroke="#222" stroke-width="1.5"/>

          <!-- rim highlight -->
          <ellipse cx="160" cy="70" rx="105" ry="14" fill="#1c1c1c" stroke="#2a2a2a" stroke-width="1"/>
          <ellipse cx="160" cy="70" rx="95" ry="10" fill="#0a0a0a"/>

          <!-- handle -->
          <path d="M 265 140 Q 320 140 320 200 Q 320 260 265 260" 
                fill="none" stroke="#1a1a1a" stroke-width="20" stroke-linecap="round"/>
          <path d="M 265 140 Q 310 140 310 200 Q 310 260 265 260" 
                fill="none" stroke="#0d0d0d" stroke-width="12" stroke-linecap="round"/>

          <!-- side metallic edge highlight -->
          <line x1="56" y1="74" x2="46" y2="335" stroke="#2f2f2f" stroke-width=".8"/>
          <line x1="264" y1="74" x2="274" y2="335" stroke="#2f2f2f" stroke-width=".8"/>
        </g>

        <!-- LED SCREEN BEZEL -->
        <rect x="68" y="144" width="156" height="112" rx="8"
              fill="#000" stroke="#1a1a1a" stroke-width="1.5"/>

        <!-- LED SCREEN CONTENT -->
        <g clip-path="url(#screenClip)">
          <rect x="72" y="148" width="148" height="104" fill="url(#ledGrad)"/>

          <!-- animated pixel art effect -->
          <g opacity=".7">
            <rect x="72" y="148" width="20" height="20" fill="#00c8ff" opacity=".4">
              <animate attributeName="opacity" values=".4;.1;.4" dur="1.3s" repeatCount="indefinite"/>
            </rect>
            <rect x="112" y="168" width="20" height="20" fill="#ffffff" opacity=".3">
              <animate attributeName="opacity" values=".3;.8;.3" dur="2s" repeatCount="indefinite"/>
            </rect>
            <rect x="152" y="148" width="20" height="20" fill="#9000ff" opacity=".5">
              <animate attributeName="opacity" values=".5;.15;.5" dur="1.8s" repeatCount="indefinite"/>
            </rect>
            <rect x="192" y="168" width="20" height="20" fill="#00ffcc" opacity=".4">
              <animate attributeName="opacity" values=".4;.9;.4" dur="2.5s" repeatCount="indefinite"/>
            </rect>
          </g>

          <!-- glow overlay -->
          <rect x="72" y="148" width="148" height="104" 
                fill="url(#ledGrad)" opacity=".3" filter="url(#glow)"/>
        </g>

        <!-- screen border glow -->
        <rect x="68" y="144" width="156" height="112" rx="8"
              fill="none" stroke="#00c8ff" stroke-width=".8" opacity=".6">
          <animate attributeName="opacity" values=".6;.15;.6" dur="3s" repeatCount="indefinite"/>
        </rect>

        <!-- bottom brand engraving -->
        <text x="160" y="330" text-anchor="middle"
              font-family="'Inter',sans-serif" font-size="10" letter-spacing="4"
              fill="#2a2a2a" text-decoration="none">NEOCUP</text>
      </svg>
    </div>

    <!-- scroll hint -->
    <div class="hero-scroll">
      <div class="hero-scroll-line"></div>
      <span>Rolar</span>
    </div>
  </section>

  <!-- ===================== TECH ===================== -->
  <section class="tech" id="tech">
    <div class="tech-inner">
      <div class="tech-header reveal">
        <p class="section-label">Tecnologia</p>
        <h2 class="section-title">Uma nova forma de personalização.</h2>
        <p class="section-desc">O visor LED integrado transforma a sua caneca em uma tela de expressão pessoal. Você escolhe o que exibir — em tempo real.</p>
      </div>

      <div class="tech-grid">
        <div class="tech-item reveal" style="transition-delay:.05s">
          <span class="tech-icon">🖼️</span>
          <p class="tech-item-title">Fotos Pessoais</p>
          <p class="tech-item-desc">Exiba suas memórias favoritas diretamente da galeria do celular com fidelidade de cores surpreendente.</p>
        </div>
        <div class="tech-item reveal" style="transition-delay:.1s">
          <span class="tech-icon">🎨</span>
          <p class="tech-item-title">Artes Digitais</p>
          <p class="tech-item-desc">Ilustrações, pintura digital e arte conceitual ganham vida no visor LED com brilho e saturação ajustáveis.</p>
        </div>
        <div class="tech-item reveal" style="transition-delay:.15s">
          <span class="tech-icon">👾</span>
          <p class="tech-item-title">Pixel Arts</p>
          <p class="tech-item-desc">A estética retro dos pixels nunca foi tão elegante. Compatível com formatos .png, .gif e .webp.</p>
        </div>
        <div class="tech-item reveal" style="transition-delay:.2s">
          <span class="tech-icon">🌟</span>
          <p class="tech-item-title">Personagens</p>
          <p class="tech-item-desc">Dê vida ao seu personagem favorito, mascote ou avatar em um suporte que vai a todos os lugares com você.</p>
        </div>
        <div class="tech-item reveal" style="transition-delay:.25s">
          <span class="tech-icon">😂</span>
          <p class="tech-item-title">Memes</p>
          <p class="tech-item-desc">Transforme o momento em humor. A NeoCup Vision é o único acessório que garante risos antes do primeiro gole.</p>
        </div>
        <div class="tech-item reveal" style="transition-delay:.3s">
          <span class="tech-icon">🏷️</span>
          <p class="tech-item-title">Logos & Marcas</p>
          <p class="tech-item-desc">Perfeito para branding pessoal e corporativo. Exiba seu logo com presença em qualquer reunião.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== HOW ===================== -->
  <section class="how" id="how">
    <div class="how-inner">
      <div class="how-header reveal">
        <p class="section-label" style="text-align:center">Como funciona</p>
        <h2 class="section-title" style="text-align:center">Três passos. Infinitas possibilidades.</h2>
        <p class="section-desc">Configurar a sua NeoCup Vision é tão simples quanto enviar uma foto para um amigo.</p>
      </div>

      <div class="how-steps">
        <div class="how-step reveal" style="transition-delay:.05s">
          <div class="how-step-num">01</div>
          <div class="how-step-icon">📱</div>
          <h3 class="how-step-title">Escolha uma imagem</h3>
          <p class="how-step-desc">Abra o app NeoCup e selecione qualquer imagem da sua galeria — fotos, artes ou capturas de tela.</p>
        </div>
        <div class="how-step reveal" style="transition-delay:.12s">
          <div class="how-step-num">02</div>
          <div class="how-step-icon">📡</div>
          <h3 class="how-step-title">Envie pelo aplicativo</h3>
          <p class="how-step-desc">Com um toque, o app transmite a imagem via conexão sem fio diretamente para a sua caneca.</p>
        </div>
        <div class="how-step reveal" style="transition-delay:.2s">
          <div class="how-step-num">03</div>
          <div class="how-step-icon">✨</div>
          <h3 class="how-step-title">Visualize instantaneamente</h3>
          <p class="how-step-desc">Em menos de 2 segundos, o visor LED exibe a imagem com cores vivas e nitidez extraordinária.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== GALLERY ===================== -->
  <section class="gallery" id="gallery">
    <div class="gallery-inner">
      <div class="gallery-header reveal">
        <p class="section-label">Galeria de demonstração</p>
        <h2 class="section-title">Veja o visor em ação.</h2>
        <p class="section-desc">Cada imagem é uma possibilidade. Cada caneca, uma tela diferente.</p>
      </div>

      <div class="gallery-grid">
        <!-- Card 1: Arte Futurista -->
        <div class="gallery-card reveal" style="transition-delay:.05s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen screen-1"></div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Arte Futurista</div>
        </div>

        <!-- Card 2: Neon landscape -->
        <div class="gallery-card reveal" style="transition-delay:.1s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen screen-2"></div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Paisagem Neon</div>
        </div>

        <!-- Card 3: Pixel Art -->
        <div class="gallery-card reveal" style="transition-delay:.15s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen screen-3" id="pixelScreen"></div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Pixel Art</div>
        </div>

        <!-- Card 4: Personagem -->
        <div class="gallery-card reveal" style="transition-delay:.2s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen screen-4"></div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Personagem</div>
        </div>

        <!-- Card 5: Meme -->
        <div class="gallery-card reveal" style="transition-delay:.25s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen screen-5"></div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Meme 😂</div>
        </div>

        <!-- Card 6: Logo -->
        <div class="gallery-card reveal" style="transition-delay:.3s">
          <div class="gallery-card-inner">
            <div class="mock-cup">
              <div class="mock-cup-body">
                <div class="mock-screen" style="background:#000;display:flex;align-items:center;justify-content:center;">
                  <div style="width:36px;height:36px;border-radius:50%;background:var(--neon);box-shadow:0 0 14px var(--neon);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:#000;">NC</div>
                </div>
              </div>
              <div class="mock-cup-handle"></div>
            </div>
          </div>
          <div class="gallery-card-label">Logo Pessoal</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== SPECS ===================== -->
  <section class="specs" id="specs">
    <div class="specs-inner">
      <div class="specs-header reveal">
        <p class="section-label">Especificações</p>
        <h2 class="section-title">Engenharia pensada em cada detalhe.</h2>
      </div>

      <div class="specs-grid">
        <div class="spec-card reveal" style="transition-delay:.05s">
          <div class="spec-icon">💡</div>
          <p class="spec-name">Visor LED Colorido</p>
          <p class="spec-detail">Painel LED AMOLED miniaturizado com 16 milhões de cores e brilho adaptativo.</p>
          <span class="spec-badge">Full Color</span>
        </div>
        <div class="spec-card reveal" style="transition-delay:.1s">
          <div class="spec-icon">📡</div>
          <p class="spec-name">Conexão App</p>
          <p class="spec-detail">Protocolo de comunicação proprietário de baixa latência, compatível com iOS e Android.</p>
          <span class="spec-badge">Wireless</span>
        </div>
        <div class="spec-card reveal" style="transition-delay:.15s">
          <div class="spec-icon">⚡</div>
          <p class="spec-name">Tempo Real</p>
          <p class="spec-detail">Atualização de imagem em menos de 2 segundos após o envio pelo aplicativo móvel.</p>
          <span class="spec-badge">&lt; 2s</span>
        </div>
        <div class="spec-card reveal" style="transition-delay:.2s">
          <div class="spec-icon">🏆</div>
          <p class="spec-name">Estrutura Premium</p>
          <p class="spec-detail">Corpo dupla parede em aço inox 18/8 com isolamento térmico de até 12 horas.</p>
          <span class="spec-badge">Inox 18/8</span>
        </div>
        <div class="spec-card reveal" style="transition-delay:.25s">
          <div class="spec-icon">🖤</div>
          <p class="spec-name">Acabamento Fosco</p>
          <p class="spec-detail">Revestimento powder coat preto fosco resistente a arranhões, impressões digitais e umidade.</p>
          <span class="spec-badge">Matte Black</span>
        </div>
        <div class="spec-card reveal" style="transition-delay:.3s">
          <div class="spec-icon">🔋</div>
          <p class="spec-name">Baixo Consumo</p>
          <p class="spec-detail">Tecnologia de display eficiente com autonomia de até 72h em modo standby.</p>
          <span class="spec-badge">72h Bateria</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== TESTIMONIALS ===================== -->
  <section class="testimonials" id="reviews">
    <div class="testimonials-inner">
      <div class="testimonials-header reveal">
        <p class="section-label">Depoimentos</p>
        <h2 class="section-title">O que dizem sobre a NeoCup Vision.</h2>
      </div>

      <div class="testimonials-track">
        <div class="testimonial-card reveal" style="transition-delay:.05s">
          <p class="testimonial-stars">★★★★★</p>
          <p class="testimonial-quote">"Parece um produto vindo do futuro. Mostrei para todo o escritório e todo mundo quis uma. A qualidade do visor é impressionante."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">👨‍💻</div>
            <div>
              <p class="testimonial-name">Lucas Mendes</p>
              <p class="testimonial-role">Designer de Produto · São Paulo</p>
            </div>
          </div>
        </div>

        <div class="testimonial-card reveal" style="transition-delay:.1s">
          <p class="testimonial-stars">★★★★★</p>
          <p class="testimonial-quote">"A personalização é incrível. Troco a imagem todo dia conforme meu humor. Hoje tá com meu gato, amanhã vai ter arte."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">👩‍🎨</div>
            <div>
              <p class="testimonial-name">Mariana Costa</p>
              <p class="testimonial-role">Artista Digital · Rio de Janeiro</p>
            </div>
          </div>
        </div>

        <div class="testimonial-card reveal" style="transition-delay:.15s">
          <p class="testimonial-stars">★★★★★</p>
          <p class="testimonial-quote">"Comprei para usar no escritório com minha logo e virou objeto de desejo de toda a equipe. Qualidade de construção excepcional."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">👨‍💼</div>
            <div>
              <p class="testimonial-name">Rafael Oliveira</p>
              <p class="testimonial-role">CEO · StartupBR</p>
            </div>
          </div>
        </div>

        <div class="testimonial-card reveal" style="transition-delay:.2s">
          <p class="testimonial-stars">★★★★★</p>
          <p class="testimonial-quote">"O aplicativo é muito intuitivo e a latência é quase zero. Fiz a troca de imagem na frente de um cliente e ele ficou boquiaberto."</p>
          <div class="testimonial-author">
            <div class="testimonial-avatar">👩‍💻</div>
            <div>
              <p class="testimonial-name">Fernanda Lima</p>
              <p class="testimonial-role">Engenheira de Software · Curitiba</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== CTA ===================== -->
  <section class="cta-section" id="cta">
    <div class="reveal">
      <p class="section-label cta-label">Disponível agora</p>
      <h2 class="cta-title">Leve sua criatividade para todos os lugares.</h2>
      <span class="cta-price">A partir de R$ 599,00 · Frete grátis · Garantia 2 anos</span>
      <button class="btn-primary" style="font-size:1rem;padding:18px 52px"
        onclick="alert('Em breve disponível! Cadastre seu e-mail para ser o primeiro a saber.')">
        Adquirir NeoCup Vision
      </button>
    </div>
  </section>

  <!-- ===================== FOOTER ===================== -->
  <footer>
    <div class="footer-inner">
      <div class="footer-top">
        <!-- brand -->
        <div>
          <p class="footer-brand-name">
            <span style="color:var(--neon)">●</span> NeoCup Vision
          </p>
          <p class="footer-brand-desc">Reimaginando o objeto mais presente das suas manhãs. Design inteligente para pessoas que não se contentam com o comum.</p>
          <div class="footer-social">
            <a href="#" aria-label="Instagram">IG</a>
            <a href="#" aria-label="Twitter / X">𝕏</a>
            <a href="#" aria-label="YouTube">▶</a>
            <a href="#" aria-label="TikTok">TT</a>
          </div>
        </div>

        <!-- links -->
        <div>
          <p class="footer-col-title">Produto</p>
          <ul class="footer-col-links">
            <li><a href="#tech">Tecnologia</a></li>
            <li><a href="#how">Como Funciona</a></li>
            <li><a href="#specs">Especificações</a></li>
            <li><a href="#gallery">Galeria</a></li>
          </ul>
        </div>

        <div>
          <p class="footer-col-title">Suporte</p>
          <ul class="footer-col-links">
            <li><a href="#">Central de Ajuda</a></li>
            <li><a href="#">Download do App</a></li>
            <li><a href="#">Garantia</a></li>
            <li><a href="#">Devoluções</a></li>
          </ul>
        </div>

        <div>
          <p class="footer-col-title">Empresa</p>
          <ul class="footer-col-links">
            <li><a href="#">Sobre Nós</a></li>
            <li><a href="#">Blog</a></li>
            <li><a href="#">Imprensa</a></li>
            <li><a href="#">Contato</a></li>
          </ul>
        </div>
      </div>

      <div class="footer-bottom">
        <p class="footer-copy">© 2025 NeoCup Vision. Todos os direitos reservados.</p>
        <div class="footer-legal">
          <a href="#">Privacidade</a>
          <a href="#">Termos de Uso</a>
          <a href="#">Cookies</a>
        </div>
      </div>
    </div>
  </footer>

  <!-- ===================== JAVASCRIPT ===================== -->
  <script>
    /* ----- Navbar scroll state ----- */
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 40);
    }, { passive: true });

    /* ----- Mobile menu ----- */
    const hamburger = document.getElementById('hamburger');
    const mobileMenu = document.getElementById('mobileMenu');

    hamburger.addEventListener('click', () => {
      hamburger.classList.toggle('open');
      mobileMenu.classList.toggle('open');
      document.body.style.overflow = mobileMenu.classList.contains('open') ? 'hidden' : '';
    });

    function closeMobile() {
      hamburger.classList.remove('open');
      mobileMenu.classList.remove('open');
      document.body.style.overflow = '';
    }

    /* ----- Scroll reveal ----- */
    const reveals = document.querySelectorAll('.reveal');

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });

    reveals.forEach(el => observer.observe(el));

    /* ----- Pixel art screen generator ----- */
    (function generatePixelArt() {
      const screen = document.getElementById('pixelScreen');
      if (!screen) return;
      const cols = 8;
      const rows = 6;
      const palette = ['#00c8ff','#9000ff','#ff00aa','#00ffcc','#ffffff','#050505','#0050ff','#ff6600'];
      for (let i = 0; i < cols * rows; i++) {
        const cell = document.createElement('div');
        cell.className = 'pixel';
        cell.style.background = palette[Math.floor(Math.random() * palette.length)];
        screen.appendChild(cell);
      }
    })();

    /* ----- Smooth anchor scroll with offset for fixed nav ----- */
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        const target = document.querySelector(this.getAttribute('href'));
        if (!target) return;
        e.preventDefault();
        const offset = 64;
        const top = target.getBoundingClientRect().top + window.scrollY - offset;
        window.scrollTo({ top, behavior: 'smooth' });
      });
    });

    /* ----- Subtle parallax on hero cup ----- */
    const heroCup = document.querySelector('.hero-cup-wrap');
    window.addEventListener('scroll', () => {
      if (!heroCup) return;
      const y = window.scrollY * 0.18;
      heroCup.style.transform = `translateY(${y}px)`;
    }, { passive: true });
  </script>
</body>
</html>